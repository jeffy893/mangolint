# Install dependencies to run server
poetry install

# Run the server on localhost:8000
poetry uvicorn server:app --reload

# Add the extension
Go to chrome://extensions and add the extension/ folder

